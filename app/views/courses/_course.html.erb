<div id="<%= dom_id course %>">
  <p>
    <strong>Name:</strong>
    <%= course.nome %>
  </p>
  <h3>Tasks</h3>
  <div id="tasks">
    <% course.foods.each do |food| %>
      <%= render partial: "foods/course_food", locals: { food: food } %>
    <% end %>
  </div>
</div>